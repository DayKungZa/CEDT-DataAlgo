#include <bits/stdc++.h>
using namespace std;

int main(){
	int n;
	cin >> n;
	map<string, string> names;
	for(int i=0;i<n;i++){
		string a,b;
		cin >> a >> b;
		map.insert({a,b});
	}
	cout << map.find("Day");
}
