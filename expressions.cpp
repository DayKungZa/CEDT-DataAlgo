#include <bits/stdc++.h>
using namespace std;

int main(){
  double n, m, o, l;
  n = M_PI - tgamma(11)/pow(8.0,8.0);
  o = 7.0/sqrt(71.0) - sin(40.0/180.0*M_PI);
  m = pow(log(9.7),o);
  l = pow(1.2, cbrt(2.3));
  cout << n*m/l;
}
