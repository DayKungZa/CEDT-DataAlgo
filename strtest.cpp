#include <bits/stdc++.h>
using namespace std;

int main(){
	string u = "computer";
	string v = "computing";
	string w = "compute";
	if(u>v) cout << u;
	else cout << v;
	if(v>w) cout << v;
	else cout << w;
	if(u>w) cout << u;
	else cout << w;
	cout << endl;
	int x = 4, t = 2, n;
	cin >> n;
	if (n < x);
	{
	    t += 2*n;
	}
	t += n;
	cout << t;
}
