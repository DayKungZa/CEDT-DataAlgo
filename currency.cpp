#include <bits/stdc++.h>
using namespace std;

double search(string name){
	name;
}

int main(){
	vector<pair<string,double>> cur;
	int n;
	double val;
	string name;
	cin >> n;
	for(int i=0;i<n;i++){
		cin >> name >> val;
		cur.push_back(make_pair(name,val));
	}
	
	return 0;
}
